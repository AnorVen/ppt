'use client'
import PeopleInfo from '@/app/people/[name]/page';
import { getToken } from '@/utils';

const Profile = ({ params })=>{
	return(
		<>
			<div>Мой профиль</div>
		</>


	)
}
 export default Profile